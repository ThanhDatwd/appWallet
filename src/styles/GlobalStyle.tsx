import {StyleSheet} from 'react-native';

export const GlobalStyle = StyleSheet.create({
  button: {
    width: '100%',
    borderRadius: 16,
    borderWidth: 1,
    borderColor: '#fff',
    borderStyle: 'solid',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    textAlign: 'center',
    paddingTop: 8,
    paddingBottom: 8,
    paddingRight: 16,
    paddingLeft: 16,
    fontSize: 16,
    fontWeight: '600',
    color: '#fff',
  },
});
